import { Metadata } from "next";

export const metadata: Metadata = {
  title: "BookSphere Book Create",
  description: "BookSphere || Book Craete Section",
};

const page = () => {
  return <section className="grid h-dvh place-items-center"></section>;
};

export default page;
