import { Metadata } from "next";

export const metadata: Metadata = {
  title: "BookSphere Author ",
  description: "BookSphere || Author Login Section",
};

const page = () => {
  return <section className="grid h-dvh place-items-center"></section>;
};

export default page;
